# 1. Metasploit Framework

### Key Terms

* **Exploit** → Code that takes advantage of a vulnerability.
* **Vulnerability** → Weakness or flaw in a system that can be exploited.
* **Payload** → Code executed on the target after successful exploitation (e.g., shell, command execution).

---

# Metasploit Modules

## 1. Auxiliary

* Used for **scanning, enumeration, fuzzing, crawling**.
* **Does NOT exploit** vulnerabilities.

**Examples:** Scanner, Crawler, Fuzzer

---

## 2. Encoders

* Encode **payloads/exploits**.
* Helps bypass **signature-based antivirus** (limited success).

---

## 3. Evasion

* Attempts to **bypass antivirus/security software**.

---

## 4. Exploits

* Modules that **exploit vulnerabilities** in target systems.

**Examples:** Windows, Linux, Android, Unix exploits.

---

## 5. NOPs (No Operation)

* CPU performs **no action** for one cycle.
* Used as **buffers** to maintain payload size and reliability.

---

## 6. Payloads

* Code executed **after successful exploitation**.

**Examples:**

* Open a shell
* Run a command
* Add a user
* Launch `calc.exe`
* Install malware/backdoor

### Types of Payloads

#### ➡️Adapters

* Convert payloads into different formats.
* Example: Wrap payload into a **PowerShell** command.

#### ➡️Singles (Inline Payloads)

* Self-contained payloads.
* No additional download required.

**Examples:** Add user, launch Notepad.

#### ➡️Stagers

* Establish connection between **Metasploit ↔ Target**.
* Downloads the actual payload (**Stage**).

#### ➡️Stages

* Main payload downloaded by the stager.
* Allows larger payloads.

---

## Single vs Staged Payload

### ➡️Single (Inline) Payload

```text
generic/shell_reverse_tcp
```

* Uses **_ (underscore)**
* Entire payload sent at once.

### ➡️Staged Payload

```text
windows/x64/shell/reverse_tcp
```

* Uses **/ (slash)**
* Sends **Stager first → then downloads Stage**.

> **Memory Trick:**
> **`_` = Single Payload**
> **`/` = Staged Payload**

---

## 7. Post Modules

* Used **after gaining access** to the target.
* Helps in **post-exploitation** activities.

**Examples:**

* Credential dumping
* Privilege escalation
* System enumeration
* Data collection
<img width="870" height="487" alt="image (32)" src="https://github.com/user-attachments/assets/36eb0789-12ba-4509-9d70-ffda5c6424df" />


---

# 2.🖥️ Metasploit Console (`msfconsole`)

**`msfconsole`** is the main interface used to work with the **Metasploit Framework**.

```bash
msfconsole
```

---

# 📌 Basic Commands

| Command          | Purpose                       |
| ---------------- | ----------------------------- |
| `help`           | Show all commands             |
| `help <command>` | Help for a specific command   |
| `history`        | Show previously used commands |
| `clear`          | Clear terminal                |
| `version`        | Show Metasploit version       |
| `exit`           | Exit Metasploit               |

---

# 🔍 Search Modules

Search modules by keyword.

```bash
search apache
search ms17-010
search telnet
```

### Search Filters

```bash
search type:auxiliary telnet
search type:exploit apache
search platform:windows smb
```

---

# 📂 Use a Module

Load a module.

```bash
use exploit/windows/smb/ms17_010_eternalblue
```

or using search result number

```bash
use 2
```

---

# ⚙️ Show Module Options

Displays required parameters.

```bash
show options
```

Common options:

* `RHOSTS` → Target IP
* `RPORT` → Target Port
* `LHOST` → Your IP
* `LPORT` → Listening Port

---

# ✏️ Set Values

```bash
set RHOSTS 10.10.10.5
set RPORT 445
set LHOST 10.10.10.20
```

---

# 📦 Show Payloads

Shows compatible payloads.

```bash
show payloads
```

---

# ℹ️ Module Information

Shows author, description, references, targets, options, etc.

```bash
info
```

or

```bash
info exploit/windows/smb/ms17_010_eternalblue
```

---

# 🔙 Go Back

Exit current module.

```bash
back
```

---

# ▶️ Run Module

Start the exploit/scanner.

```bash
run
```

or

```bash
exploit
```

---

# 🧠 Module Context

When you load a module:

```bash
use exploit/windows/smb/ms17_010_eternalblue
```

Prompt changes from

```text
msf6 >
```

to

```text
msf6 exploit(windows/smb/ms17_010_eternalblue) >
```

➡️ You are now inside that module.

**Note:** Settings like `RHOSTS` and `RPORT` are **module-specific**. If you switch to another module, you'll need to set them again.

---

# ⭐ Exploit Ranking (Reliability)

| Rank      | Meaning               |
| --------- | --------------------- |
| Excellent | Very reliable         |
| Great     | Highly reliable       |
| Good      | Reliable              |
| Normal    | Works in many cases   |
| Average   | May fail sometimes    |
| Low       | Unreliable            |
| Manual    | Requires manual steps |

---

# 📝 Most Important Commands

```bash
msfconsole          # Start Metasploit
search <keyword>    # Search modules
use <module>        # Load module
show options        # Show required options
set <option> <val>  # Set value
show payloads       # Show payloads
info                # Module details
run                 # Execute module
back                # Exit module
history             # Command history
help                # Help
exit                # Quit Metasploit
```

---

## 🎯 TryHackMe Answers

**Q. Search for a module related to Apache**

```bash
search apache
```

**Q. Who provided `auxiliary/scanner/ssh/ssh_login`?**

```bash
info auxiliary/scanner/ssh/ssh_login
```
Haan 😅 woh theory important hai, especially agar GitHub notes bana rahi ho. Add this section **before the `use exploit/windows/smb/ms17_010_eternalblue` command**.

---

# 💥 EternalBlue (MS17-010)

**EternalBlue** is an **SMBv1 (Server Message Block v1)** vulnerability discovered by the **U.S. National Security Agency (NSA)**.

* 📅 **Disclosed:** 2017
* 🎯 **Target:** Microsoft Windows systems
* 🔓 **Port Used:** **445 (SMB)**
* ⚠️ **Impact:** Allows **Remote Code Execution (RCE)** on vulnerable Windows machines.

### 🛠️ Why is it dangerous?

An attacker can execute malicious code on the victim's computer **without physical access**, simply by exploiting the SMB vulnerability.

---

# 🦠 WannaCry Ransomware

**WannaCry** is a **ransomware attack** that spread worldwide in **May 2017** using the **EternalBlue exploit**.

### What it did:

* 🔒 Encrypted users' files
* 💰 Demanded ransom payment in Bitcoin
* 🌍 Infected **200,000+ computers** across **150+ countries**

### Attack Flow

```text
EternalBlue Vulnerability (MS17-010)
            ↓
   Exploits SMBv1 (Port 445)
            ↓
 Gains Remote Code Execution
            ↓
    WannaCry Executes
            ↓
 Encrypts Files & Demands Ransom
```


---

# 03. 🚀 Metasploit Framework working with modules - Parameters & Sessions

> 💡 **Goal:** Learn how to configure modules, run exploits, and manage sessions in Metasploit.

---

## 📍 1. Setting Parameters

Every Metasploit module requires parameters before execution.

**Syntax**

```
set PARAMETER VALUE
```

🔹 Verify configured parameters:

```
show options
```

> ⚠️ **Always check `show options` before running an exploit.**

---

## 📍 2. Metasploit Prompts

| Prompt                     | Meaning                 |
| -------------------------- | ----------------------- |
| 💻 `root@...#`             | Normal Linux Terminal   |
| 🎯 `msf6 >`                | Main Metasploit Console |
| 📦 `msf6 exploit(...) >`   | Inside a Module         |
| ⚡ `meterpreter >`          | Meterpreter Session     |
| 🖥️ `C:\Windows\system32>` | Target Machine Shell    |

> 💡 The prompt tells you **where you are currently working.**

---

## 📍 3. Important Parameters

| Parameter      | Purpose                          |
| -------------- | -------------------------------- |
| 🎯 **RHOSTS**  | Target IP Address                |
| 🌐 **RPORT**   | Target Service Port              |
| 💣 **PAYLOAD** | Code executed after exploitation |
| 🖥️ **LHOST**  | Your (Attacker) IP               |
| 🔌 **LPORT**   | Listening Port                   |
| 📡 **SESSION** | Existing Meterpreter Session     |

---

## 📍 4. Managing Parameters

### ✅ Set Parameter

```
set RHOSTS 10.10.x.x
```

### ❌ Remove Parameter

```
unset PAYLOAD
```

### 🧹 Remove All Parameters

```
unset all
```

---

## 📍 5. Global Parameters

Instead of configuring the same value repeatedly:

```
setg RHOSTS 10.10.x.x
```

Remove it anytime:

```
unsetg RHOSTS
```

> ⭐ **`set` = Current Module**
> ⭐ **`setg` = All Modules**

---

## 📍 6. Running Modules

| Command      | Purpose                             |
| ------------ | ----------------------------------- |
| `exploit`    | Run Exploit                         |
| `run`        | Same as `exploit`                   |
| `exploit -z` | Run & Background Session            |
| `check`      | Verify Vulnerability (if supported) |

---

## 📍 7. Sessions

A **Session** is the communication channel between Metasploit and the compromised target.

### Useful Commands

```
background
```

➡️ Send current session to background.

```
sessions
```

➡️ List all active sessions.

```
sessions -i <ID>
```

➡️ Interact with a specific session.

---

## 📝 Quick Revision

✔️ `show options` → View parameters

✔️ `set` → Configure module

✔️ `setg` → Configure globally

✔️ `unset` → Remove one value

✔️ `unset all` → Remove all values

✔️ `exploit` / `run` → Execute module

✔️ `exploit -z` → Execute & background session

✔️ `background` → Send session to background

✔️ `sessions` → View active sessions

✔️ `sessions -i` → Connect to a session

---

## 🎯 TryHackMe Answers

| Question           | Answer                    |
| ------------------ | ------------------------- |
| Set LPORT to 6666  | `set LPORT 6666`          |
| Set global RHOSTS  | `setg RHOSTS 10.10.19.23` |
| Clear payload      | `unset PAYLOAD`           |
| Start exploitation | `exploit`                 |

---

# 4. 🎯 Room Summary

Metasploit simplifies the **complete exploitation process** by providing ready-to-use modules.

### 🔄 Exploitation Workflow

1. 🔍 **Find the Exploit** – Search for a suitable exploit module.
2. ⚙️ **Customize the Exploit** – Configure required parameters (e.g., `RHOSTS`, `LHOST`, `PAYLOAD`).
3. 🚀 **Exploit the Target** – Execute the module to gain access.

---

## 🛠️ What You Learned

* ✅ Basics of the **Metasploit Framework**
* ✅ Different types of **Metasploit modules**
* ✅ Setting and managing module parameters
* ✅ Running exploits using `exploit` / `run`
* ✅ Managing active Meterpreter sessions
* ✅ Using the **`ms17_010_eternalblue`** module to exploit a vulnerable machine

---

## 📝 Key Takeaways

* ⭐ Metasploit automates the exploitation process.
* ⭐ Every exploit requires proper parameter configuration before execution.
* ⭐ Modules make tasks like scanning, exploitation, payload delivery, and post-exploitation much easier.
* ⭐ Session management is an essential part of working with compromised systems.

---

## ⚡ Quick Revision

📌 **Exploitation Process:**
**Find Exploit → Configure Parameters → Execute Exploit**

---

> 💡 **Revision Tip:** Remember the **3-step workflow**: **Search → Configure → Exploit**. Most Metasploit usage follows this sequence.
